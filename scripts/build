#!/usr/bin/env bash
# WinTune build — use this from Git Bash, not build.ps1 (PowerShell only).
set -euo pipefail
ROOT="$(cd "$(dirname "$0")/.." && pwd)"
exec powershell.exe -NoProfile -ExecutionPolicy Bypass -File "$ROOT/scripts/build.ps1" "$@"
