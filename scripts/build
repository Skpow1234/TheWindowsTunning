#!/usr/bin/env bash
# WinTune build — Git Bash / WSL / MSYS wrapper (calls build.ps1).
# Do not run build.ps1 with bash directly.
set -euo pipefail
ROOT="$(cd "$(dirname "$0")" && pwd)"
exec "$ROOT/_invoke-ps1.sh" build.ps1 "$@"
